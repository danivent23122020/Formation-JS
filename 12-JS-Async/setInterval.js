/**
 * Utiliser setInterval()
 * méthode directement intégrée dans JavaScript
 * et que se répète à un interval fixe
 */
let compteur = 0;

// setInterval(incr, 1000);

function incr() {
    compteur++;
    console.log(compteur);
}
